version: '3.8'

services:
  giveme:
    build: .
    container_name: giveme-app
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    volumes:
      - ./data:/app/data
    environment:
      - APP_PASSWORD=${APP_PASSWORD:-changeme}
      - PORT=${PORT:-5000}
    restart: unless-stopped
